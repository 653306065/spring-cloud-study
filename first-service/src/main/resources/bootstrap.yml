   
eureka:
   client:
      serviceUrl:
         defaultZone: http://127.0.0.1:10001/eureka/

spring:
   application:
      name: first-service 
   cloud:
      config:
          label: master
          profile:    
          discovery:
             enabled: true
             serviceId: config-service        